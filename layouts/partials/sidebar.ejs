<div class="sidebar">
	<div class="widgets">
		// <script>
		//   (function() {
		//     var cx = '011965042345812424928:qicnsbm1oyk';
		//     var gcse = document.createElement('script');
		//     gcse.type = 'text/javascript';
		//     gcse.async = true;
		//     gcse.src = 'https://cse.google.com/cse.js?cx=' + cx;
		//     var s = document.getElementsByTagName('script')[0];
		//     s.parentNode.insertBefore(gcse, s);
		//   })();
		// </script>
		// <section class="widget-search">
		// 	<gcse:search></gcse:search>
		// </section>
	// 	<% if (typeof site.dates !== "undefiend") {%>
	// 		<section class="widget-archive">
	// 			<h2 class="widget-title">Archive</h2>
	// 			<ul>
	// 			<%
	// 			var years = Object.keys(site.dates).map(year=>{
	// 				var months = Object.keys(site.dates[year]).map(month=> {
	// 					return month
	// 				})
	// 				months = months.sort((a,b)=> {
	// 					return a - b
	// 				})
	// 				obj = {}
	// 				obj[year] = months
	// 				return obj
	// 			}).sort((a,b)=>{
	// 				return Object.keys(a)[0] - Object.keys(b)[0]
	// 			})
	// 			for (yearindex of years) {
	// 				var year = Object.keys(yearindex)[0]
	// 				%>
	// 				<li>
	// 					<div class="archive-year"><%=year%></div>
	// 					<ul>
	// 						<%for (month of yearindex[year]) {%>
	// 							<li><div class="archive-month"><%=moment(site.dates[year][month].date.split("/").join("-")).format('MM MMMM')%></div>
	// 							<ul>
	// 								<%for (file in site.dates[year][month].files) {
	// 									var post = site.dates[year][month].files[file]%>
	// 									<li>
	// 									<div class="archive-title"><a href="<%=post.permalink%>"><%=post.title%></a></div>
	// 									</li>
	// 								<%}%>
	// 							</ul>
	// 							</li>
	// 						<%}%>
	// 					</ul>
	// 				</li>
	// 			<%}%>
	// 			<ul>
	// 		</section>
	// 	<%}%>
	// 	<%if (typeof site.tags !== "undefined") {%>
	// 		<%var txnms = []
	// 		for (item in site.tags) {
	// 			txnms.push(site.tags[item])
	// 		}
	// 		txnms = txnms.sort((a,b)=>{
	// 			if (a.nicename < b.nicename) {
	// 				return -1;
	// 			}
	// 			if (a.nicename > b.nicename) {
	// 				return 1;
	// 			}
	// 			return 0;
	// 		});
	// 		var largest_txnm = txnms.reduce((a, b) => {
	// 			var a = a.count || a
	// 			var b = b.count || b
	// 			if (a > b) {return a}
	// 			if (b > a) {return b}
	// 			return a + b;})
	// 		var largest = 1.5
	// 		var smallest = 0.7
	// 		var steps = largest - smallest
	// 		for (item = 0; item < txnms.length; item++) {
	// 			let importance = Math.log(txnms[item].count) / Math.log(largest_txnm)
	// 			var fontsize = importance * steps + smallest
	// 			fontsize = fontsize.toFixed(1)
	// 		}%>
	// 		<section class="widget widget-tags">
	// 			<h2 class="widget-title">Tags/Categories</h2>
	// 			<div class="tagcloud">
	// 				<a href="/<%=txnms[item].type%>/<%=txnms[item].nicename%>" style="font-size: <%= fontsize%>em;"><%=txnms[item].name%></a>
	// 			</div>
	// 		</section>
	// 	<%}%>
    // </div>
</div>
